# Stimma numpy ports of upstream ComfyUI-Darkroom nodes that are torch-only
# upstream. Unlike vendor/ (byte-identical copies), files here are rewrites
# over numpy/scipy, each mirroring one upstream file at the commit recorded
# in ../vendor/ATTRIBUTION.md. On re-vendor, diff the upstream originals
# against that commit to find changes to fold in.
