<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Chromium/V8 under the hardened runtime. -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.allow-dyld-environment-variables</key>
	<true/>
	<!-- Nested payloads (portable Python, onnxruntime dylibs) are not
	     Team-ID-signed libraries; the backend interpreter loads them. -->
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<!-- Dictation (stimma-native) captures the microphone; matches the Tauri
	     shell's Entitlements.plist. -->
	<key>com.apple.security.device.audio-input</key>
	<true/>
</dict>
</plist>
