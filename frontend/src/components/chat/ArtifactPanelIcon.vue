<template>
  <!-- "Right panel" glyph: a frame with the right third split off. Shared by
       the control-strip toggle (panel closed) and the stage header (panel open)
       so the same control visibly moves between the two places. -->
  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
    <rect x="3" y="4.5" width="18" height="15" rx="2" stroke-linecap="round" stroke-linejoin="round" />
    <path stroke-linecap="round" d="M15 4.5v15" />
  </svg>
</template>
